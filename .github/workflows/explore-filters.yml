name: Workflow filters and activities

on:
    workflow_dispatch: 
    push:
        branches: 
            - main
            - '!feature/*'
        # branches-ignore:
        #    - test/**
        #    - feature/*
    
    pull_request: 
        types: 
            - opened
            - closed

        paths-ignore:
            - README.md 
        branches:
            - main

jobs:
    hello:
        runs-on: ubuntu-latest
        steps:
            - run: echo this workflow/job/sten is execute for event type ${{ github.event_name }}     
